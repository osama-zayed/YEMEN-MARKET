<!-- Breadcrumb Start -->
<div class="container-fluid d-flex pt-1  bg-light">
    <div class="row px-xl-5">
        <div class="col-12">
            {{-- <nav class="breadcrumb bg-light">
                @if ($activePage == 'cart_page')
                    <a class="breadcrumb-item text-dark" href="{{ route('Product.index') }}">{{__("private.Home")}}</a>
                    <a class="breadcrumb-item text-dark" href="{{ route('cartPage') }}">cart_page</a>
                @endif
                @if ($activePage == 'cart')
                    <a class="breadcrumb-item text-dark" href="{{ route('Product.index') }}">{{__("private.Home")}}</a>
                    <a class="breadcrumb-item text-dark" href="{{ route('cart') }}">{{__("private.cart")}}</a>
                @endif
                @if ($activePage == 'Shop')
                    <a class="breadcrumb-item text-dark" href="{{ route('Product.index') }}">{{__("private.Home")}}</a>
                    <a class="breadcrumb-item text-dark" href="{{ route('home') }}">{{__("private.Shop")}}</a>
                @endif
            </nav> --}}
        </div>
    </div>
</div>
<!-- Breadcrumb End -->
